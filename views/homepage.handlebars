{{#each blogs as |blog| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{{get_emoji}}} {{blog.title}}</h2>
  </div>
  <div class="col-md-6">
    <h3><a href="mailto:{{blog.description}}">{{blog.description}}</a></h3>
  </div>
</div>
{{/each}}
